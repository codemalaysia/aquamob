<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="LoginEditTextTheme">
        <!-- Used for the bottom line when not selected / focused -->
        <item name="colorControlNormal">#9e9e9e</item>
        <!-- colorControlActivated & colorControlHighlight use the colorAccent color by default -->
        <item name="colorControlActivated">#fff</item>
        <item name="colorControlHighlight">#fff</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar"/>

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light"/>

    <style name="FieldHeaderStyle" parent="TextAppearance.AppCompat">
        <item name="android:textSize">@dimen/field_header_size</item>
        <item name="android:textColor">#000</item>
        <item name="android:paddingBottom">2dp</item>
    </style>

    <style name="FieldSubHeaderStyle" parent="TextAppearance.AppCompat">
        <item name="android:textSize">@dimen/field_subheader_size</item>
        <item name="android:textColor">#000</item>
    </style>

</resources>
